version: '3'
services:
  db:
    image: postgres
  web:
    build: .
    volumes:
      - .:/notes-api
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      PGHOST: "db"
      PGUSER: "postgres"
      PGDBNAME: "notes-api"
    depends_on:
      - db